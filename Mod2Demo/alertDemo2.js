// to use alerts in JS, use the alert() function

// to get an input using an alert, use window.prompt()
var userName = window.prompt("Enter your name: ");
// use an alert() to display the name entered
alert(`Hello ${userName}!`);